# =============================================================================
# Cloudflare Pages headers — equivalent to the Netlify netlify.toml block.
# Applied at the edge to every response matching the path pattern.
# =============================================================================

# Security headers — applied to every response.
/*
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=(), usb=(), accelerometer=(), gyroscope=(), magnetometer=()
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self' data:; img-src 'self' data: blob:; frame-src https://www.openstreetmap.org; connect-src 'self'; base-uri 'self'; form-action 'self' mailto: https://wa.me; frame-ancestors 'none'; upgrade-insecure-requests
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin

# Hashed Vite assets (JS, CSS) — content-addressed, immutable, cache 1 year.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Photos — long cache (1 year), filename change forces invalidation.
/photos/*
  Cache-Control: public, max-age=31536000, immutable

# Self-hosted fonts — long cache.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# Favicon — moderate cache.
/favicon.svg
  Cache-Control: public, max-age=604800

# OG image — moderate cache.
/og-image.*
  Cache-Control: public, max-age=604800

# sitemap.xml / robots.txt — short cache, content does change.
/sitemap.xml
  Cache-Control: public, max-age=3600

/robots.txt
  Cache-Control: public, max-age=86400

# Web manifest — moderate cache + correct content type.
/manifest.webmanifest
  Cache-Control: public, max-age=604800
  Content-Type: application/manifest+json; charset=utf-8

# llms.txt + llms-full.txt — markdown for AI engines (Perplexity, ChatGPT, Claude, Gemini).
# Served with text/plain so they don't get download-prompted in browsers.
/llms.txt
  Cache-Control: public, max-age=3600
  Content-Type: text/plain; charset=utf-8

/llms-full.txt
  Cache-Control: public, max-age=3600
  Content-Type: text/plain; charset=utf-8

# Markdown sibling of the home page — used by AI scrapers that follow /index.md
# or /page.md conventions (Anthropic, Stripe pattern).
/index.md
  Cache-Control: public, max-age=3600
  Content-Type: text/markdown; charset=utf-8
